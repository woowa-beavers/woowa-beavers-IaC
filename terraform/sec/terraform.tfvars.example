# terraform/sec/terraform.tfvars.example
# 역할: sec 루트 모듈 변수 값 예시 (실제 사용 시 terraform.tfvars 로 복사)
# 흐름: 예시 값 확인 → terraform.tfvars 생성 → terraform apply 시 variables.tf 로 주입

aws_region = "ap-northeast-2"
